{
  "ignoreCommand": "git diff --quiet HEAD^ HEAD ./",
  "buildCommand": "vite build",
  "outputDirectory": ".output",
  "framework": null,
  "crons": [{ "path": "/api/cron/cuotas", "schedule": "0 14 * * *" }]
}
